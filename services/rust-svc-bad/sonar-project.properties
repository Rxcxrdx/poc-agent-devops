# SonarCloud configuration for rust-svc-bad
# Replace SONAR_ORG with your SonarCloud organization slug
#
# NOTE: This service deliberately violates BOX-001/002/003 conformance rules.
# SonarCloud will PASS this service (BOX violations are not standard Sonar rules).
# Only the AI conformance gate will detect and block it.

sonar.projectKey=conformance-gate_rust-svc-bad
sonar.organization=SONAR_ORG

sonar.projectName=rust-svc-bad
sonar.projectVersion=0.1.0

sonar.sources=src
sonar.sourceEncoding=UTF-8
